{% extends 'base.html.twig' %}

{% block title %}Tableau de bord Recruteur - PITCREW{% endblock %}

{% block body %}
<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <div class="md:flex md:items-center md:justify-between">
        <div class="flex-1 min-w-0">
            <h2 class="text-2xl font-bold leading-7 text-gray-900 sm:text-3xl sm:truncate">
                Tableau de bord
            </h2>
        </div>
        <div class="mt-4 flex md:mt-0 md:ml-4">
            <a href="{{ path('app_recruiter_job_offer_new') }}"
               class="ml-3 inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                Publier une offre
            </a>
        </div>
    </div>

    <div class="mt-8">
        <div class="md:grid md:grid-cols-3 md:gap-6">
            <!-- Statistiques -->
            <div class="md:col-span-1">
                <div class="bg-white shadow rounded-lg p-6">
                    <h3 class="text-lg font-medium text-gray-900">Statistiques</h3>
                    <dl class="mt-5 grid grid-cols-1 gap-5">
                        <div class="bg-gray-50 px-4 py-5 sm:p-6 rounded-lg">
                            <dt class="text-sm font-medium text-gray-500">Offres actives</dt>
                            <dd class="mt-1 text-3xl font-semibold text-blue-600">{{ activeOffers|length }}</dd>
                        </div>
                        <div class="bg-gray-50 px-4 py-5 sm:p-6 rounded-lg">
                            <dt class="text-sm font-medium text-gray-500">Offres expirées</dt>
                            <dd class="mt-1 text-3xl font-semibold text-gray-600">{{ expiredOffers|length }}</dd>
                        </div>
                    </dl>
                </div>

                <!-- Nouveau bloc: Matching Candidats -->
                <div class="bg-white shadow rounded-lg p-6 mt-6">
                    <div class="flex items-center justify-between">
                        <h3 class="text-lg font-medium text-gray-900">Matching Candidats</h3>
                        <a href="{{ path('app_matching_dashboard_recruiter') }}" class="text-sm text-blue-600 hover:text-blue-800">
                            Voir tout
                        </a>
                    </div>
                    <div class="mt-4">
                        <p class="text-sm text-gray-500">Trouvez les candidats les plus compatibles avec vos offres grâce à notre algorithme de matching avancé.</p>
                        
                        {% if activeOffers is empty %}
                            <div class="mt-3 bg-yellow-50 p-3 rounded-lg">
                                <p class="text-sm text-yellow-700">Publiez des offres d'emploi pour voir les candidats recommandés.</p>
                            </div>
                        {% else %}
                            <div class="mt-4">
                                <a href="{{ path('app_matching_dashboard_recruiter') }}" 
                                   class="block w-full bg-blue-50 hover:bg-blue-100 text-blue-700 text-center py-3 px-4 rounded-md text-sm font-medium transition duration-150 ease-in-out">
                                    Découvrir les candidats compatibles
                                </a>
                            </div>
                        {% endif %}
                    </div>
                </div>
            </div>

            <!-- Liste des offres -->
            <div class="mt-5 md:mt-0 md:col-span-2">
                <div class="bg-white shadow rounded-lg">
                    <div class="p-6">
                        <h3 class="text-lg font-medium text-gray-900">Offres actives</h3>
                        {% if activeOffers is empty %}
                            <p class="mt-4 text-sm text-gray-500">Aucune offre active pour le moment.</p>
                        {% else %}
                            <div class="mt-4 space-y-4">
                                {% for offer in activeOffers %}
                                    <div class="bg-gray-50 rounded-lg p-4">
                                        <div class="flex items-center justify-between">
                                            <div>
                                                <h4 class="text-lg font-medium text-gray-900">{{ offer.title }}</h4>
                                                <p class="mt-1 text-sm text-gray-500">{{ offer.location }} - {{ offer.contractType }}</p>
                                            </div>
                                            <div class="flex space-x-2">
                                                <a href="{{ path('app_recruiter_job_offer_edit', {id: offer.id}) }}"
                                                   class="inline-flex items-center px-3 py-1.5 border border-gray-300 shadow-sm text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                                                    Modifier
                                                </a>
                                                <a href="{{ path('app_recruiter_job_offer_toggle', {id: offer.id}) }}"
                                                   class="inline-flex items-center px-3 py-1.5 border border-gray-300 shadow-sm text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                                                    Désactiver
                                                </a>
                                            </div>
                                        </div>
                                        <div class="mt-2">
                                            <p class="text-sm text-gray-500">{{ offer.description|slice(0, 150) }}...</p>
                                        </div>
                                        {% if offer.applications is not empty %}
                                            <div class="mt-2">
                                                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                                    {{ offer.applications|length }} candidature(s)
                                                </span>
                                                <a href="{{ path('app_job_application_recruiter') }}" class="ml-2 text-sm text-blue-600 hover:text-blue-800">
                                                    Voir les candidatures
                                                </a>
                                            </div>
                                        {% endif %}
                                        <!-- Ajout du bouton de matching -->
                                        <div class="mt-2">
                                            <a href="{{ path('app_matching_suggestions_job_offer', {id: offer.id}) }}" class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800 hover:bg-blue-200">
                                                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1" viewBox="0 0 20 20" fill="currentColor">
                                                    <path fill-rule="evenodd" d="M10 2a1 1 0 011 1v1.323l3.954 1.582 1.599-.8a1 1 0 01.894 1.79l-1.233.616 1.738 5.42a1 1 0 01-.285 1.05A3.989 3.989 0 0115 15a3.989 3.989 0 01-2.667-1.019 1 1 0 01-.285-1.05l1.715-5.349L11 6.477V16h2a1 1 0 110 2H7a1 1 0 110-2h2V6.477L6.237 7.582l1.715 5.349a1 1 0 01-.285 1.05A3.989 3.989 0 015 15a3.989 3.989 0 01-2.667-1.019 1 1 0 01-.285-1.05l1.738-5.42-1.233-.616a1 1 0 01.894-1.79l1.599.8L9 4.323V3a1 1 0 011-1z" clip-rule="evenodd" />
                                                </svg>
                                                Trouver les candidats compatibles
                                            </a>
                                        </div>
                                    </div>
                                {% endfor %}
                            </div>
                        {% endif %}
                    </div>
                </div>

                {% if expiredOffers is not empty %}
                    <div class="mt-6 bg-white shadow rounded-lg">
                        <div class="p-6">
                            <h3 class="text-lg font-medium text-gray-900">Offres expirées</h3>
                            <div class="mt-4 space-y-4">
                                {% for offer in expiredOffers %}
                                    <div class="bg-gray-50 rounded-lg p-4">
                                        <div class="flex items-center justify-between">
                                            <div>
                                                <h4 class="text-lg font-medium text-gray-500">{{ offer.title }}</h4>
                                                <p class="mt-1 text-sm text-gray-500">{{ offer.location }} - {{ offer.contractType }}</p>
                                            </div>
                                            <div class="flex space-x-2">
                                                <a href="{{ path('app_recruiter_job_offer_toggle', {id: offer.id}) }}"
                                                   class="inline-flex items-center px-3 py-1.5 border border-gray-300 shadow-sm text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                                                    Réactiver
                                                </a>
                                            </div>
                                        </div>
                                    </div>
                                {% endfor %}
                            </div>
                        </div>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
</div>
{% endblock %} 