{% extends 'admin/layout.html.twig' %}

{% block title %}Nouvel abonnement recruteur{% endblock %}

{% block content_title %}Ajouter un abonnement recruteur{% endblock %}

{% block page_actions %}
    <a href="{{ path('admin_recruiter_subscription_index') }}" class="btn btn-secondary">
        <i class="fas fa-arrow-left"></i> Retour à la liste
    </a>
{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title">Informations de l'abonnement</h5>
                </div>
                <div class="card-body">
                    {{ include('admin/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %} 