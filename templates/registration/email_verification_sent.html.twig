{% extends 'base.html.twig' %}

{% block title %}Email de vérification envoyé{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow">
                    <div class="card-header bg-primary text-white">
                        <h2 class="text-center mb-0">Email de vérification envoyé</h2>
                    </div>
                    <div class="card-body">
                        <div class="text-center mb-4">
                            <i class="fas fa-envelope-open-text fa-5x text-primary mb-3"></i>
                        </div>
                        
                        <h4 class="text-center mb-3">Un email de confirmation a été envoyé à votre adresse email.</h4>
                        
                        <div class="alert alert-info">
                            <p>Veuillez vérifier votre boîte de réception et cliquer sur le lien de confirmation pour activer votre compte.</p>
                            <p>Si vous ne trouvez pas l'email dans votre boîte de réception, vérifiez votre dossier de spam.</p>
                        </div>

                        <div class="mt-4 text-center">
                            <p>Vous n'avez pas reçu l'email de confirmation ?</p>
                            <a href="{{ path('app_resend_verification_email') }}" class="btn btn-primary">
                                Renvoyer l'email de vérification
                            </a>
                        </div>

                        {% if app.environment == 'dev' %}
                            <div class="alert alert-warning mt-4">
                                <h5 class="alert-heading">Mode développement</h5>
                                <p>En mode développement, les emails sont interceptés par MailHog.</p>
                                <p>Vous pouvez les consulter à l'adresse : <a href="http://localhost:8025" target="_blank">http://localhost:8025</a></p>
                                <hr>
                                <a href="{{ path('app_debug_email_config') }}" class="btn btn-sm btn-secondary">
                                    Page de débogage des emails
                                </a>
                            </div>
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %} 